export function DashboardPage() {
  return (
    <div className="space-y-3">
      <h1 className="text-xl font-semibold">Dashboard</h1>
      <p className="text-sm text-[var(--ds-muted)]">
        Product shell is running. Next: Jobs → Job Detail → WOF/Invoice flow.
      </p>
    </div>
  );
}
